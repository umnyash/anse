extends layout/layout

block variables
  - var title = 'Завершенные заказы | Anse'
  - var page = 'completed-orders'

block content
  +orders({
    orders: {
      completed: true,
    },
    activeLink: 'completed'
  })

block modals
  +modal(class='modal--default modal--with_manager-contacts' data-modal='manager-contacts')
    +manager-contacts
