mixin products-slider({ products, headingTag })
  .products-slider.swiper&attributes(attributes)
    ul.products-slider__list.swiper-wrapper
      each product in products
        li.products-slider__item.swiper-slide
          +product-card({ product, headingTag })
    .products-slider__pagination.slider-pagination
